# Desarrollo: usar ruta relativa para que Vite haga proxy a Spring (puerto 8080) y no bloquee CORS.
# Producción: URL completa, p. ej. https://api.tudominio.cl/api/v1
VITE_API_URL=/api/v1
VITE_APP_NAME=Somos Barrio
VITE_ENV=development